apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: {{ .Release.Name }}-mcp-role

rules:
  - apiGroups: [""]
    resources:
      - pods/log
      - services
      - endpoints
      - events
      - namespaces
    verbs:
      - get
      - list
      - watch

  - apiGroups: ["apps"]
    resources:
      - deployments
      - replicasets
    verbs:
      - get
      - list
      - watch