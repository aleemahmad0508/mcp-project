apiVersion: v1
kind: Service

metadata:
  name: flask
  namespace: flask

spec:

  selector:
    app: flask

  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP

  type: {{ .Values.service.type }}